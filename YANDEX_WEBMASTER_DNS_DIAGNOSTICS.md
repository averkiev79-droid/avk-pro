# Диагностика проблемы DNS в Яндекс.Вебмастере

## Дата проверки
18 ноября 2025 г.

## Краткое резюме
Сайт **avk-pro.ru** функционирует корректно и доступен из интернета. DNS-записи резолвятся правильно, все основные файлы (robots.txt, sitemap.xml, RSS feed) доступны и возвращают корректные данные. Проблема, о которой сообщает Яндекс.Вебмастер, скорее всего связана с проксированием Cloudflare или региональными особенностями доступа.

---

## Выполненная диагностика

### 1. Проверка DNS-резолвинга
```bash
getent hosts avk-pro.ru
# Результат: 162.159.142.117 avk-pro.ru
# ✅ DNS корректно резолвится в IP-адрес Cloudflare
```

### 2. Проверка доступности сайта
```bash
curl -I https://avk-pro.ru
# Результат: HTTP/2 200
# ✅ Сайт доступен и возвращает корректный ответ
```

### 3. Проверка с User-Agent бота Яндекса
```bash
curl -I -A "Mozilla/5.0 (compatible; YandexBot/3.0; +http://yandex.com/bots)" https://avk-pro.ru
# Результат: HTTP/2 200
# ✅ Сайт корректно отвечает боту Яндекса
```

### 4. Проверка robots.txt
```bash
curl -I -A "Mozilla/5.0 (compatible; YandexBot/3.0; +http://yandex.com/bots)" https://avk-pro.ru/robots.txt
# Результат: HTTP/2 200
# Content-Type: text/plain; charset=utf-8
# ✅ robots.txt доступен
```

**Содержимое robots.txt:**
- User-agent: Yandex - **разрешен** (Allow: /)
- Sitemap: https://avk-pro.ru/sitemap.xml - **указан корректно**
- Нет блокировок для Яндекс-бота

### 5. Проверка sitemap.xml
```bash
curl -I -A "Mozilla/5.0 (compatible; YandexBot/3.0; +http://yandex.com/bots)" https://avk-pro.ru/sitemap.xml
# Результат: HTTP/2 200
# Content-Type: application/xml; charset=utf-8
# ✅ sitemap.xml доступен и возвращает корректный XML
```

### 6. Проверка RSS Feed
```bash
curl -s -A "Mozilla/5.0 (compatible; YandexBot/3.0; +http://yandex.com/bots)" https://avk-pro.ru/api/rss
# Результат: Корректный XML RSS feed
# ✅ RSS доступен через GET-запрос
```

**Примечание:** RSS endpoint возвращает 405 (Method Not Allowed) при HEAD-запросе, что является нормальным поведением для API, поддерживающего только GET.

### 7. Проверка Cloudflare
- ✅ Сайт работает через прокси Cloudflare (server: cloudflare)
- ✅ SSL/TLS работает корректно
- ✅ Нет явных блокировок по User-Agent

---

## Выявленные и исправленные проблемы

### ❌ Проблема #1: API endpoint в sitemap.xml (ИСПРАВЛЕНО)
**Описание:** В sitemap.xml был указан `/api/rss` - это API-эндпоинт, который не должен индексироваться поисковыми системами.

**Исправление:** Удалена запись `/api/rss` из sitemap.xml. API-эндпоинты не должны находиться в sitemap, так как они не являются HTML-страницами для пользователей.

**Статус:** ✅ ИСПРАВЛЕНО

### ✅ RSS Feed корректно подключен
- RSS feed указан в `<head>` HTML-документа (строка 28 в index.html)
- Endpoint `/api/rss` работает и возвращает корректный XML
- Ссылка также указана в robots.txt через sitemap

---

## Возможные причины проблемы с Яндекс.Вебмастером

### 1. Cloudflare Proxy (Наиболее вероятная причина)
**Описание:** Cloudflare проксирует весь трафик, что может создавать проблемы для ботов Яндекса при первичном обходе сайта.

**Рекомендации:**
1. Зайти в панель управления Cloudflare
2. Перейти в раздел **DNS**
3. Временно отключить проксирование (оранжевое облачко → серое) для записи A домена avk-pro.ru
4. Дождаться обновления DNS (обычно 5-15 минут)
5. В Яндекс.Вебмастере запросить повторное сканирование сайта
6. После успешной индексации можно снова включить прокси Cloudflare

**Альтернатива:** В настройках Cloudflare создать Page Rule для User-Agent YandexBot с отключенным прокси.

### 2. Региональные ограничения
**Описание:** Cloudflare может иметь временные проблемы с доступом из определенных регионов или для определенных диапазонов IP.

**Рекомендации:**
1. Проверить в панели Cloudflare раздел **Security → WAF** на наличие правил, блокирующих российские IP
2. Проверить **Security → Bots** - убедиться, что Яндекс-бот не заблокирован
3. В разделе **Firewall Rules** убедиться, что нет правил, блокирующих трафик из России

### 3. SSL/TLS настройки
**Рекомендации:**
1. В Cloudflare перейти в **SSL/TLS → Edge Certificates**
2. Убедиться, что **Minimum TLS Version** установлена на **TLS 1.2** (или ниже, если нужна совместимость со старыми ботами)
3. Проверить, что **Always Use HTTPS** включен

### 4. Временная DNS-пропагация
**Описание:** Возможно, на момент проверки Яндекс.Вебмастером были временные проблемы с DNS.

**Рекомендации:**
1. Подождать 24-48 часов для полной пропагации DNS
2. Запросить повторное сканирование в Яндекс.Вебмастере
3. Проверить DNS-записи у регистратора домена

---

## Дополнительные рекомендации

### 1. Проверка в Яндекс.Вебмастере
1. Зайти в **Инструменты → Проверка ответа сервера**
2. Ввести URL: `https://avk-pro.ru`
3. Посмотреть детальный отчет о доступности
4. Если есть конкретные ошибки - скриншот для дальнейшей диагностики

### 2. Мониторинг доступности
Использовать внешние сервисы для проверки доступности сайта из разных регионов:
- https://www.host-tracker.com/
- https://uptimerobot.com/
- https://ping.pe/avk-pro.ru

### 3. Проверка DNS-пропагации
Использовать сервисы для проверки глобальной пропагации DNS:
- https://dnschecker.org/ - проверить домен avk-pro.ru
- https://www.whatsmydns.net/ - проверить из разных стран

### 4. Настройка Cloudflare для SEO
Рекомендуемые настройки в Cloudflare:
- **Speed → Optimization → Auto Minify**: включить для CSS, JS, HTML
- **Caching → Configuration → Browser Cache TTL**: установить на 4 hours
- **Network → HTTP/3**: включить
- **Security → Bot Fight Mode**: выключить (может блокировать легитимных ботов)

---

## Краткий план действий

1. ✅ **ВЫПОЛНЕНО:** Исправлен sitemap.xml (удален API endpoint)
2. 🔄 **РЕКОМЕНДУЕТСЯ:** Временно отключить прокси Cloudflare на 24 часа для первичной индексации
3. 🔄 **РЕКОМЕНДУЕТСЯ:** Проверить настройки безопасности Cloudflare (WAF, Firewall Rules, Bots)
4. 🔄 **РЕКОМЕНДУЕТСЯ:** В Яндекс.Вебмастере запросить повторное сканирование через 24 часа
5. 🔄 **РЕКОМЕНДУЕТСЯ:** Мониторить доступность сайта через внешние сервисы

---

## Заключение

Технически сайт **avk-pro.ru** полностью функционален и доступен:
- ✅ DNS резолвится корректно
- ✅ Сайт доступен по HTTPS
- ✅ Все SEO-файлы (robots.txt, sitemap.xml, RSS) работают корректно
- ✅ Яндекс-бот не заблокирован на уровне robots.txt

**Вероятная причина проблемы:** Проксирование Cloudflare или временные региональные проблемы с доступом.

**Рекомендуемое решение:** Временно отключить прокси Cloudflare (оранжевое облачко → серое) на 24-48 часов для первичной индексации Яндексом, затем снова включить.

---

## Контакты для поддержки

Если проблема сохраняется после выполнения рекомендаций:
1. Обратиться в техподдержку Cloudflare с описанием проблемы
2. Обратиться в поддержку Яндекс.Вебмастера через форму обратной связи
3. Предоставить скриншот ошибки из Яндекс.Вебмастера для более точной диагностики
