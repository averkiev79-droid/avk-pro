═══════════════════════════════════════════════════════════
  🚀 БЫСТРАЯ ИНСТРУКЦИЯ ПО ДЕПЛОЮ AVK-PRO.RU
═══════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────┐
│  ШАГ 1: СОХРАНИТЬ В GITHUB (на платформе Emergent)     │
└─────────────────────────────────────────────────────────┘

  → Нажать кнопку "Save to Github"
  → Дождаться подтверждения

┌─────────────────────────────────────────────────────────┐
│  ШАГ 2: ЗАПУСТИТЬ ДЕПЛОЙ (в PuTTY)                     │
└─────────────────────────────────────────────────────────┘

  1. Подключиться к серверу:
     ssh root@avk-pro.ru

  2. Запустить деплой:
     cd /var/www/avk-pro && bash deploy.sh

  3. Дождаться завершения (~30 секунд)

  4. Проверить сайт: https://avk-pro.ru

═══════════════════════════════════════════════════════════

📋 ЧАСТЫЕ КОМАНДЫ

  • Статус сервисов:
    sudo supervisorctl status

  • Логи backend:
    sudo tail -f /var/log/supervisor/avk-pro-backend.err.log

  • Перезапуск backend:
    sudo supervisorctl restart avk-pro-backend

  • Перезапуск Nginx:
    sudo systemctl reload nginx

  • Откат на бэкап:
    ls /var/www/backups/avk-pro/
    rsync -a /var/www/backups/avk-pro/backup_XXXXXXXX/ /var/www/avk-pro/
    sudo supervisorctl restart all

═══════════════════════════════════════════════════════════

⚠️  ПРИ ПРОБЛЕМАХ

  1. Проверить логи:
     sudo tail -n 100 /var/log/supervisor/avk-pro-backend.err.log

  2. Перезапустить сервисы:
     sudo supervisorctl restart all

  3. Откатиться на бэкап (см. выше)

═══════════════════════════════════════════════════════════

✅ ВСЁ! Деплой занимает 30 секунд ⚡

Полная документация: /var/www/avk-pro/DEPLOY_INSTRUCTIONS.md
