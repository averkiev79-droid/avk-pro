# 🚀 Инструкция по деплою AVK-PRO.RU

## Процесс деплоя (2 шага)

### Шаг 1: Сохранение изменений в GitHub (на платформе Emergent)

1. После завершения разработки нажмите кнопку **"Save to Github"** в интерфейсе Emergent
2. Дождитесь подтверждения, что изменения сохранены в репозиторий
3. Все изменения будут закоммичены и запушены в GitHub автоматически

---

### Шаг 2: Запуск деплоя на сервере (через PuTTY)

#### 2.1. Подключение к серверу

```bash
# Откройте PuTTY и подключитесь к серверу
ssh root@avk-pro.ru
# или
ssh username@your-server-ip
```

#### 2.2. Первый раз: Копирование скрипта на сервер

**Вариант A: Скрипт уже в репозитории**

Скрипт `deploy.sh` сохранён в корне проекта. После первого `git pull` он будет на сервере.

```bash
cd /var/www/avk-pro
chmod +x deploy.sh
```

**Вариант B: Ручное создание скрипта** (если его нет в репозитории)

```bash
cd /var/www/avk-pro
nano deploy.sh
# Скопируйте содержимое скрипта и сохраните (Ctrl+O, Enter, Ctrl+X)
chmod +x deploy.sh
```

#### 2.3. Запуск деплоя

```bash
cd /var/www/avk-pro
bash deploy.sh
```

Или короткая команда:

```bash
cd /var/www/avk-pro && bash deploy.sh
```

---

## Что делает скрипт?

1. ✅ **Создаёт бэкап** текущей версии в `/var/www/backups/avk-pro/`
2. ✅ **Сохраняет .env файлы** (чтобы не потерять пароли и ключи)
3. ✅ **Делает git pull** из GitHub
4. ✅ **Восстанавливает .env файлы**
5. ✅ **Проверяет изменения** в `requirements.txt` и `package.json`
6. ✅ **Устанавливает новые зависимости** (только если они изменились)
7. ✅ **Перезапускает backend** (через supervisor)
8. ✅ **Перезапускает frontend** (через PM2 или supervisor)
9. ✅ **Перезагружает Nginx**
10. ✅ **Проверяет работоспособность** сайта

---

## Пример вывода скрипта

```bash
========================================
  🚀 Деплой AVK-PRO.RU
========================================

📦 Шаг 1: Создание бэкапа...
✅ Бэкап создан: /var/www/backups/avk-pro/backup_20231119_153045

📥 Шаг 2: Получение обновлений из GitHub...
✅ Обновления получены

📦 Шаг 3: Проверка зависимостей...
✅ Backend зависимости не изменились
✅ Frontend зависимости не изменились

🔄 Шаг 5: Перезапуск сервисов...
  - Перезапуск backend...
  ✅ Backend запущен
  - Перезапуск frontend (Supervisor)...
  ✅ Frontend перезапущен
  - Перезагрузка Nginx...
  ✅ Nginx перезагружен

🔍 Шаг 6: Проверка работоспособности...
✅ Backend отвечает
✅ Frontend отвечает
✅ Сайт доступен: https://avk-pro.ru (HTTP 200)

========================================
✅ Деплой завершён успешно!
========================================
```

---

## Настройка скрипта

Если ваша конфигурация отличается, отредактируйте переменные в начале скрипта:

```bash
nano /var/www/avk-pro/deploy.sh
```

Измените:
```bash
PROJECT_DIR="/var/www/avk-pro"      # Путь к проекту
BACKUP_DIR="/var/www/backups/avk-pro"  # Путь к бэкапам
BRANCH="main"                        # Ветка Git (main или master)
```

---

## Полезные команды

### Проверка статуса сервисов

```bash
# Статус всех сервисов
sudo supervisorctl status

# Статус конкретного сервиса
sudo supervisorctl status avk-pro-backend
```

### Просмотр логов

```bash
# Логи backend (последние 50 строк)
sudo tail -n 50 /var/log/supervisor/avk-pro-backend.err.log

# Логи backend в реальном времени
sudo tail -f /var/log/supervisor/avk-pro-backend.err.log

# Логи Nginx
sudo tail -f /var/log/nginx/error.log
```

### Ручной перезапуск сервисов

```bash
# Перезапуск backend
sudo supervisorctl restart avk-pro-backend

# Перезапуск frontend
sudo supervisorctl restart avk-pro-frontend

# Перезапуск Nginx
sudo nginx -t && sudo systemctl reload nginx
```

### Откат на предыдущую версию

Если что-то пошло не так:

```bash
# Найти последний бэкап
ls -lt /var/www/backups/avk-pro/ | head -5

# Откатиться на бэкап
rsync -a /var/www/backups/avk-pro/backup_20231119_153045/ /var/www/avk-pro/

# Перезапустить сервисы
sudo supervisorctl restart all
```

---

## Решение проблем

### Проблема: Backend не запускается

```bash
# Проверить логи
sudo tail -n 100 /var/log/supervisor/avk-pro-backend.err.log

# Проверить supervisor конфиг
cat /etc/supervisor/conf.d/avk-pro-backend.conf

# Перечитать конфиги и перезапустить
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl restart avk-pro-backend
```

### Проблема: Сайт показывает старую версию

```bash
# Очистить кэш браузера (Ctrl+F5)

# Перезагрузить Nginx
sudo systemctl reload nginx

# Проверить, что файлы обновились
cd /var/www/avk-pro
git log --oneline -5
```

### Проблема: Git конфликты

```bash
cd /var/www/avk-pro

# Посмотреть статус
git status

# Сбросить локальные изменения
git reset --hard origin/main

# Или stash изменения
git stash save "backup-$(date +%Y%m%d)"

# Затем запустить деплой снова
bash deploy.sh
```

---

## Безопасность

⚠️ **Важно:**

1. **Не храните пароли в Git** - файлы `.env` исключены из репозитория
2. **Скрипт автоматически сохраняет .env** перед git pull
3. **Бэкапы создаются перед каждым деплоем** на случай проблем
4. **Права на скрипт**: только root или sudo пользователи могут запускать

---

## Автоматизация (опционально)

Для автоматического деплоя при каждом push в GitHub:

### Вариант 1: GitHub Actions

Создайте `.github/workflows/deploy.yml` в репозитории.

### Вариант 2: GitHub Webhooks + скрипт

Настройте webhook, который будет вызывать скрипт при push.

---

## Контакты

При проблемах с деплоем:
- Проверьте логи: `/var/log/supervisor/`
- Проверьте статус: `sudo supervisorctl status`
- Откатитесь на бэкап если нужно

---

**Готово!** Теперь деплой занимает 2 шага и ~30 секунд ⚡
